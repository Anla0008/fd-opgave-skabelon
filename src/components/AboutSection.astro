---
import HeadingButton from "./HeadingButton.astro";
---

<section>
    <div class="wrapper">
        <div class="vision-container">
            <div class="vision-text">
                <HeadingButton style="primary">OUR VISION</HeadingButton>
                <h2>Turn your ideas into reality.</h2>
                <p>Capitalize on low hanging fruit to identify a ballpark value added activity beta test. Override the digital divide with additional from DevOps.</p>
                <ul>
                    <li>Bring to the table win-win survival strategies to ensure proactive domination. At the end of the day.</li>
                    <li>Capitalize on low hanging fruit to identify a ballpark value added activity to beta test override the digital.</li>
                </ul>
            </div>
            <div class="vision-img">
                <img src="/gallery/case2.jpg" alt="Case Image" />
                <div class="decorative-box"></div>
            </div>
        </div>
    </div>
</section>

<style>
    section {
        padding: 4rem 0;
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: var(--secondary02-color); /* Hvid */
    }

    .vision-container {
        display: grid;
        gap: 2rem;
        grid-template-columns: 1fr 1fr;
        align-items: center;
        max-width: 1200px;
        margin: 0 auto;
    }

    .vision-text {
        display: flex;
        flex-direction: column;
        justify-content: center;
    }

    h2 {
        color: var(--primary03-color); /* Sort tekst */
        font-size: var(--font-size-h2); /* H2 størrelse fra globale variabler */
        font-family: var(--font-primary); /* Primær skrifttype */
        font-weight: var(--font-weight-bold); /* Bold vægt */
        line-height: var(--line-height-h4); /* Højdeforhold */
        margin-bottom: 1rem;
    }

    p {
        color: var(--primary03-color); /* Sort tekst */
        font-size: var(--font-size-p); /* Brødtekst størrelse */
        font-family: var(--font-secondary); /* Sekundær skrifttype */
        line-height: var(--line-height-p); /* Linjehøjde */
        margin-bottom: 2rem;
    }

    ul {
        list-style: none;
        padding: 0;
    }

    li {
        display: flex;
        align-items: center;
        gap: 1rem;
        font-size: var(--font-size-p); /* Brødtekst størrelse */
        color: var(--primary03-color); /* Sort tekst */
    }

    li:before {
        content: "";
        width: 1.6rem;
        height: 1.6rem;
        background: var(--checkmark-icon); /* Checkmark-ikon */
        background-size: contain;
        background-repeat: no-repeat;
    }

    .vision-img {
        position: relative;
    }

    .vision-img img {
        width: 100%;
        border-radius: 1.75rem;
        position: relative;
        z-index: 2;
    }

    .decorative-box {
        position: absolute;
        right: -2rem;
        top: -2rem;
        width: 10rem;
        height: 10rem;
        background-color: var(--primary02-color); /* Grøn */
        border-radius: 1rem;
        z-index: 1;
        transform: rotate(-90deg);
    }

    @media (max-width: 800px) {
        .vision-container {
            grid-template-columns: 1fr;
            text-align: center;
        }

        .decorative-box {
            display: none;
        }
    }
</style>
